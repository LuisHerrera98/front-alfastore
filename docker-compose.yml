version: '3'

services:
  frontend_thealfastore:
    container_name: frontend
    image: frontend
    build:
      context: .
    ports:
      - "3001:3001"
    environment:
      - API=https://api.alfastoreargentina.com